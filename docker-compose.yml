services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: recadastro
      POSTGRES_PASSWORD: recadastro
      POSTGRES_DB: recadastramento
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
